<div class="container">
  <div class="row">
    <div class="col mt-5">
      <% @users.each do |user| %>
        <div>
          <%= link_to "#{user.name}", user_path(user) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
