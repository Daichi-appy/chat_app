<div class="container">
  <div class="row">
    <div class="col-6 mt-5 mx-auto">
    <%# カード %>
      <% @users.each do |user| %>
        <div class="card border-dark mb-3">
          <p><%= link_to "#{user.name}", user_path(user) %></p>
        </div>
      <% end %>
    </div>
  </div>
</div>
