<div class="container">
  <div class="row">
    <div class="col-6 mt-5 mx-auto">
      <% @users.each do |user| %>
        <p><%= link_to "#{user.name}", chat_path(user.id) %><p>
      <% end %>
    </div>
  </div>
</div>