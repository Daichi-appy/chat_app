<div class="container">
  <div class="row">
    <div class="col mt-5">
      <% @users.each do |user| %>
        <p><%= link_to "#{user.name}", chat_path(user.id) %><p>
      <% end %>
    </div>
  </div>
</div>